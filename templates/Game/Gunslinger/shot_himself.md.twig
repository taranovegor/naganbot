{{ 'gunslinger.shot_himself'|trans(
    intl_rand_variation_params('gunslinger.shot_himself', {
        'variation': null
    })|merge({
        '%username%': gunslinger.user.username.mention|escape('markdown')
    })
) }}
